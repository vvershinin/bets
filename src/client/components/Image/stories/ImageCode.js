import React from 'react';
import Image from '../../Image';

export const ImageSimple = () => (
    <Image
        src={ 'image-path' }
        className={ 'my-image' }
    />
);

export default ImageSimple;
