import React from 'react';
import DayNightSwitcher from '../../DayNightSwitcher';

export const DNSSimpleCode = () => (
    <DayNightSwitcher
        changeTheme={ () => { /** do something */ } }
    />
);

export default DNSSimpleCode;
