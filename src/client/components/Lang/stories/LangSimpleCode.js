import React from 'react';
import Lang from '../../Lang';

export const LangSimpleCode = () => (
    <Lang
        lang={ 'ru' }
        changeLang={ () => { /** do something */ } }
    />
);

export default LangSimpleCode;
